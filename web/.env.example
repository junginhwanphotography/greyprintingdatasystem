# API 서버 주소. 터미널에 "server listening on port N" 나오는 포트와 맞추기
# 설정 시 브라우저가 이 주소로 직접 요청 (예: API가 3001이면 아래처럼)
# VITE_API_URL=http://localhost:3001
# 설정 안 하면 상대 경로로 요청 → Vite가 프록시. 프록시 대상은 VITE_API_TARGET 사용
# VITE_API_TARGET=http://localhost:3001
